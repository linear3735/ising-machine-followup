# 精度方向严谨验证（2026-09-02，precision_ablation.py 全实证）

## 方向判断：成立，且是干净的"填补空白"扩展

王的论文（2603.13778/2507.08533）默认 FP64（numpy 惯例），全文无精度敏感性实验——
"FP64 是工程默认不是必要结论"可成立。降精度是扩展，不反驳论文。

## 实证结果（全部本会话可复现）

### A. 精度消融（解码恒用 FP64，同种子同动力学）
| 系统 | FP64 | FP32 | FP16 |
|---|---|---|---|
| SK N=100 (32跑) | −0.68677 | **−0.68677（逐位同）** | −0.68652（Δ+2.5e-4） |
| SK N=200 (32跑) | −0.70804 | **−0.70804（逐位同）** | −0.70809（Δ−5e-5） |
| SK N=1000 (16跑) | −0.7124 | **−0.7124（Δ=0）** | ±2.3e-4 |
| digCIM G1 (32跑) | Ps 28% | **Ps 28%（同）** | Ps 28%（同） |

→ **FP32 无损失（逐位一致）；FP16 均值偏移 ≤2.5e-4 = 深度 0.04%，低于逐种子噪声
（~0.02），best 不变。** best/Ps 所有精度全同。

### B. 舍入噪声迁移测试（FP64 注入受控 σ，检验 NQS σ² 界）
- 度量用解码能量（符号离散量）→ **阈值效应**：σ ≤ 1e-3 偏差精确为 0；
  之上 ~σ^1.1（best 度量，极值统计）/~σ^1.3（mean-final 度量，带符号翻转阈值）；
- **NQS 的 bias∝σ² 界不直接迁移**（它针对连续估计量），但实践裕量被实证确认——
  系统比 σ² 界更宽容（小 σ 区偏差恒 0）；
- 交叉验证：FP16 实测偏移 2.5e-4 ≈ 注入模型在 σ_fp16≈3e-4（ulp/√12）的预测
  （best-bias ~2e-4）——模型能解释真实 FP16 行为。

### C. 未测/待办（诚实边界）
1. N=10⁴ 的 SK（需服务器；FP32 累加在 N≤1000 实测为 0，外推方向安全但未证实）；
2. smomentum/动量动力学在 FP16 的消融（只测了梯度温度退火 + digCIM）；
3. **速度声称**：CPU numpy 下 FP32/FP16 无明显加速（内存带宽边际）；真实收益在
   GPU tensor core（FP16/FP8）与内存带宽 2-4×——须在目标硬件实测，不引用 FPGA 论文的 10×；
4. 符号阈值偏差律的解析推导（可作理论小节：舍入噪声经符号解码的阈值化）。

## 给王的话术（可辩护版，证据在握）

"您的数字求解器默认 FP64。我做了精度消融（同种子同动力学，解码保 FP64）：
**FP32 逐位复现结果（SK N≤1000、G1 digCIM），FP16 均值偏移 ≤0.04% 深度、best/Ps 不变**；
并发现解码能量的舍入噪声呈符号阈值效应（σ≤1e-3 偏差恒 0，比混合精度 MCMC 的 σ² 界更宽容）。
这是对论文精度敏感性空白的填补（扩展而非反驳）。下一步建议在 N=10⁴ GPU 上验证 +
测 tensor-core FP16 实际加速，并连 Danner 基准的 J 分辨率轴——面向硬件部署的必要一步。"
