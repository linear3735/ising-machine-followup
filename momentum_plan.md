# 研究计划：带动量的哈密顿动力学 Ising Machine

**—— 把 Wong 等 (arXiv:2603.13778) 的 "future work" 变成可发表的结果**

## 一、要攻克的三个开放问题

| # | 开放问题 | 论文原文依据 |
|---|---------|-------------|
| Q1 | 常数因子到底小多少？怎么随 N 变化？ | "smaller constant factors"（无定量） |
| Q2 | "额外的调度自由度"是什么、怎么调最优？ | "extra parameter scheduling"（未展开） |
| Q3 | 动量版的"有效能隙"和标度律还成立吗？ | 论文全部分析都在过阻尼（梯度）框架内 |

**已有底牌**：SK 上 TTS 快 ~20 倍（47-56 步 vs 1172-1263 步）、N=200 最优 −0.7622 逼近 −0.7632；
G1-G3 上动量输（与论文 dSB 数据一致）。这些既是起点，也是要解释的现象。

## 当前进展（2026-09-01 更新，Phase 0-1 已完成）

- **M0 ✓**：γ=0 精确复现弹道 SB；γ→∞ 平滑减速 → 时间重标度后等价梯度动力学。
- **Phase 1 G1 诊断 ✓**：弹道动量在 G1 失败机理 = 末段动能剧烈振荡 + μ=0 无法锁定，
  且对比不公平（缺 digCIM 数字化驱动项与温度噪声）。
- **smomentum_digcim（阻尼动量 × digCIM 驱动 × 温度退火 × 墙）复验（64 种子）**：
  best = **11624 = 已知最优**；γ=3, dt=0.2, 2000 步 → **Ps≈23%**（同 2000 步 digCIM
  的 6% 高 ~3.8×，与论文 5000 步的 26.6% 相当、步数少 60%）；步长 dt=0.2 ≈ 论文
  Euler 0.03 的 7× → TTS_steps ≈ 2.2× 减少（"advanced numerical algorithms" 的适度兑现）。
- **诚实更正**：此前"16 种子 0% 失败率、γ∈[3,8]×dt∈[0.2,0.4] 宽稳定窗"经复验
  不成立——实际稳定窗窄（γ∈[3,5]、dt≤0.25，dt≥0.3 发散），且"~20 倍"是 SK 动量
  TTS 的数字，不能外推到 G1。这与论文"dSB 仅大实例胜出"一致 → **Phase 2 首选
  G11-G21（大实例动量赢区）**。

## 二、核心科学假设

**假设 1（常数因子）**：动量 TTS 常数因子优势随 N 增大而放大（小图优势不显、大图显著）。

**假设 2（阻尼 = 新的调度维度）**：动量系统有梯度系统没有的旋钮——阻尼 γ：
```
过阻尼（γ 大）≈ 梯度动力学（稳但慢）
欠阻尼（γ 小）≈ 弹道 SB（快但振荡、方差大）
→ "阻尼退火"（damping annealing）＝ 一条从"稳"到"快"的新退火路径
```

**假设 3（标度迁移）**：动量版解码能量同样服从 Eq. (7)-(9) 的 N^(−2/3) × t^(−ξ) 标度，
但幂律指数 ξ 更小（更快逼近基态）——"更小常数因子"的可量化形式。

## 三、执行计划（13 周，4 阶段）

### Phase 0 · 预备（W0，1 周）
- 精读论文 §VIII future work 段 + Goto 等 dSB/bSB 参数（a0、c0、墙、步长）；
- `momentum/` 目录：cim.py 动量版加入阻尼 γ 与三种模式（纯 bSB / 阻尼 / 过阻尼→梯度极限）；
- 📤 M0：γ ∈ [0.01, 100] 连续插值，验证 γ→∞ 精确退化为梯度动力学（正确性锚点）。

### Phase 1 · 正确性 + 诊断（W1-2）
- SK N=100：γ 扫描 × 调度（线性/幂律/热启动）× 多种子 → Ps(t) 成功率曲线；
- 诊断 G1 为何输：动量在哪个阶段输（分岔前？锁定后振荡？）——用 μ 和动能轨迹；
- 📤 M1：一张"γ × 指标"图 + G1 失败机理的明确解释。

### Phase 2 · 常数因子量化（W3-5，服务器）
- N ∈ {100, 200, 500, 1000, 2000} × 200 实例 × 30 跑（复刻论文 Fig. 7 协议）；
- 每个 N：梯度 vs 动量 的 Ps(t)、TTS_steps、解码能量；
- 动量版 Eq. (7)-(9) 标度分析：N^(−2/3) 斜率是否仍为 A=0.701？t^(−ξ) 的 ξ 是否 < 0.763？
- 📤 M2：动量 vs 梯度的完整标度表——Q1 的答案。

### Phase 3 · 调度自由度（W6-8，主场）
- closed_loop.py 框架搬到动量版，动作空间 = 调度 T(t) 与 γ(t) 双通道；
- 状态：(μ=近零密度, 平均动能, 进度, 当前阻尼)——μ 与动能联合指示器（Q3 实验入口）；
- 📤 M3：学出的 γ(t) 曲线 + 可解释规律（预期：早期高阻尼防发散、过临界点后减阻尼加速）。

### Phase 4 · 打榜 + 收口（W9-13）
- 服务器：Gset G11-G21（论文显示 dSB 赢区）+ QUBO-QLIB 6 题动量版；
- 写作：方法（阻尼插值 + 阻尼退火 + 标度分析）→ 结果（γ 相图 / 标度表 / 阻尼路径）→ 讨论（与 §VIII 呼应）；
- 📤 M4：论文式报告 + 一页卡。

## 四、资源与风险

| 资源 | 需求 |
|------|------|
| 算力 | RunPod CPU 16 核（Phase 2 的 N≥1000）；2 vCPU 可跑小 N |
| 代码 | 复用 cim.py + closed_loop.py，加 γ 与动能状态 |

| 风险 | 应对 |
|------|------|
| 动量方差大（已见） | 多种子 + 最优/均值双口径报告 |
| 阻尼退火不优于固定最优阻尼 | 诚实报告，退而验证最优固定阻尼的标度律 |
| 大 N 拟合不稳 | 严格按论文协议（200 实例 × 30 跑 + 误差棒口径） |
| 理论侧（动量版 DMFT）超出能力 | 只做数值/经验侧，把理论问题作为给教授/华为的讨论提案 |

## 五、与教授讨论的三个问题

1. 动量动力学多一个速度自由度，有效方程变二阶——近零密度 μ 与动能 E_kin 是否应联合作为"动量版有效能隙"的指示器？
2. 若标度分析量化出 ξ_momentum < ξ_gradient，您预期这个差值的理论来源是什么？
3. "额外的调度自由度"您心中指的是阻尼，还是质量、墙高、非线性形状？
