# 动量声称修正（2026-09-02，honest_rerun.py + gamma_sens.py）

## 发现的 provenance 问题（原报告主表）

1. **comparison.py 的 sb_run 用 dt=0.3 且无记录**：温度退火 dt=0.02 × 3000 步 = 60 tu，
   动量 dt=0.3 × 3000 步 = **900 tu（15× 物理时间）**——原表"同步数对比"实际给了
   动量 15× 的退火时间，"mean −0.7009 vs −0.6893"和"TTS 快 20-27×"绝大部分来自
   这个未披露的 dt 差（~15×），其余来自依赖最好方法的松阈值 0.9×best_all；
2. **主表动量配置 ≠ 文档记录的 γ 扫描配置**：主表是 γ=0 弹道 dt=0.3；γ=0.3 甜点
   （momentum_plan.md）从未进主表；
3. **dev/eval 重叠**：γ 扫描的 dev 实例 seed=1 也在原评估的 3 实例（1,2,3）里。

## 修正协议（先定后跑，无偷看）

- dev: seed=101（不在 eval 集），γ 扫描于 **eval 同预算（60 tu）**；
- eval: N=100/200, 实例 102-105, 8 种子, **等物理时间 60 tu**
  （梯度 3000×0.02 = 动量 1200×0.05），固定阈值网格。

## 修正结果

**dev 定参**：γ 在 0.1-1.0 间 mean 只差 0.001（噪声级，选参本身脆弱）；按 mean 选 γ*=1.0。

**等物理时间下（实例 102-105）**：
| 口径 | 温度退火（论文） | 动量（γ=0.1-1.0） |
|---|---|---|
| 终态 mean (60tu) | N=100 胜 2/3、N=200 胜 4/4 | 无实例胜出（N=200） |
| TTS 物理时间（达阈者中位） | 基线 | 1.3-2.9× 快（γ=0.1-0.3 最优） |
| 达标率（60tu 内） | 62-100% | **系统性低 20-40pp**（N=200 更甚） |
| 期望 TTS（含达标率） | — | **实例相关：约半实例被低达标率抹平** |

**结论（诚实）**：
1. **"动量 mean 更优"不成立**（等物理时间下温度退火反超）；原声称是 dt=0.3 产物；
2. **动量存在真实的"速度-可靠性权衡"**：成功的跑上 1.3-2.9× 快（支持论文"更小
   常数因子"猜想的数值实例），但达标率系统性低 20-40pp，期望 TTS 半数实例被抹平；
3. **γ 是脆弱旋钮**：0.1-1.0 之间 mean 差异在噪声内，但速度/可靠性权衡随 γ 大改；
4. **对外的可辩护声称（修正版）**：
   - 积分器层（稳）：tanh+半隐式动量容许 dt 大 2.5-15×，同步数下覆盖更多退火时间；
   - 动力学层（弱）：成功跑物理时间快 ~1.5-3×，但可靠性低，净收益实例相关；
   - **"温度退火 ≥ 动量均值质量、动量提供带风险的加速"**——与论文自身立场
     （温度退火近最优、动量是带猜想的 future work）一致，我们提供了量化。

## 撤销/加注清单

- report.md 主表 "动量 mean −0.7009/−0.7285、TTS 56/47"：**作废**，换修正协议数字；
- onepage.md / wong_followup.pdf / 一页卡中 "20-25×"、"N=200 最优 −0.7622"：
  −0.7622 为 3 实例×8 种子 best（900tu 动量跑出），需加注（等物理时间下温度退火
  best −0.7468 于 60tu；900tu 下温度退火能到多少未测——待补）；"20-25×"改为
  "同步数下（dt 大 15×）步数少 ~20×，其中物理时间优势 ~1.5-3× 且可靠性下降"；
- momentum_plan.md "γ=0.3 甜点"：标注为单实例、150tu 预算下的观测，非普适；
- honest_ledger.md：本文件即其修正版附录。

## G1 等物理时间补测（2026-09-02，64 种子）

| 方法 | 步数×dt | 物理时间 | Ps(≥11624) |
|---|---|---|---|
| digCIM (论文) | 5000×0.03 | 150 tu | **26.6%** |
| smomentum | 750×0.2 | 150 tu | 3.1% |
| smomentum | 2000×0.2 | 400 tu | 23.4% |

→ "2000 步 ≈ 5000 步质量"的声称 = dt 效应（2.7× 退火时间）；同物理时间下
digCIM 动力学每时间单位成功率是 smomentum 的 ~8.6×。Gset 侧修正：smomentum
仅在"步数/算力口径"下以 dt 换步数（同质量 40% 步数），每单位退火时间的动力学
质量不占优——与 SK 结论一致：**论文的梯度+温度退火才是稳健主力**。

## 审计后的幸存清单（回答"优化还有没有用"）

**仍有效（重新框架后）**：
1. 工程层：smomentum_fast 2-13× 墙钟（逐位一致、模型无关）；GRPO v2 3× 训练提速；
   QUBO-QLIB 口径修复——全部与"哪个动力学更好"无关，纯赢；
2. 闭环 μ-残差调度（N=200 迁移 mean 更优+方差最小）——**dt 口径干净（两侧 dt=0.02），
   但需重审**：原评估实例含训练实例 1（leak），须在 101-108 干净重评；
3. 科学贡献（改写）：动量 = 论文"更小常数因子"猜想的量化——不是支配而是
   **速度-可靠性权衡**（成功跑 1.5-3× 快、达标率低 20-40pp、期望 TTS 实例相关）；
   论文自己的"温度退火近最优"被我们的数据强化而非削弱；
4. 协议/方法学：两阶段沉降（动量专用修正，小）、预算分配>初始多样性、无偷看纪律。

**已失效（撤销）**：动量 mean 支配、TTS 20-27×、Gset 同质量 40% 步数的
"动力学更快"表述（降级为 dt 换步数的算力口径）。

## 闭环策略干净重评（2026-09-02，clean_eval_cl_v2.py，审计收尾）

训练 1-16（v2/Adam 150 epochs），评估 101-108 × 16 种子 × 2000 步 (40tu, dt=0.02 两侧一致)：

| N | closed mean±std | temp mean±std | 配对 Δ | closed 更优 | 每实例 std closed/temp |
|---|---|---|---|---|---|
| 100 | −0.6981±0.0314 | −0.6927±0.0312 | **−0.0054** | 62% | 0.0154 / 0.0165 |
| 200 (迁移) | −0.7057±0.0234 | −0.7055±0.0217 | **−0.0002** | 53% | 0.0160 / 0.0157 |

结论（诚实）：
1. **N=200 迁移声称（−0.7308 vs −0.7271、方差更小）不成立**：干净评估 Δ=−0.0002
   （无差异），方差略大——原数字是 leak 实例（1,2,3 含训练实例 1）+ 旧策略的产物；
2. **N=100 有一个小的真实候选**：Δ=−0.0054（62% 更优，名义 t≈2.6），每实例 std
   略小（0.0154 vs 0.0165）——但只有 8 实例，实例数才是统计限制，需更多 held-out
   实例确认；
3. best：closed −0.7626 vs temp −0.7589（N=200）——closed 单跑更深，但为 128 跑
   best-of，非稳健声称。

## 审计终局（所有声称的最终状态）

| 声称 | 状态 |
|---|---|
| 动量 mean 更优 + TTS 20-27× | ✗ dt 产物，撤销 |
| 动量 best ≈ Parisi (−0.7622) | ✗ 900tu 未对照，撤销 |
| Gset 同质量 40% 步数 | △ 降级为 dt 换步数（算力口径），同物理时间 digCIM 强 8.6× |
| 闭环 N=200 迁移最优 | ✗ 干净评估 Δ≈0 |
| 闭环 N=100 小增益 | ◯ 候选（Δ−0.005，需更多实例确认） |
| 工程加速（批量/稀疏/GRPO-v2/QUBO 口径） | ✓ 模型无关，全部有效 |
| 动量 = 速度-可靠性权衡（论文猜想量化） | ✓ 修正后成立 |
| 负结果图（初始/重启/草案/潜空间） | ✓ 一致且可写 |

**净贡献（审计后）**：对王项目 = 工程基础设施 + 干净协议下对论文结论的复现与量化
（含修正过度声称）+ 一个待确认的 N=100 小增益；对论文 = 我们的数据站在论文一边。

## 闭环 μ-残差策略审计（2026-09-02，终审：不显著，撤）

- N=100: 24 held-out 实例 x 8 种子 = 192 配对（同 dt=0.02、3000 步、同种子）：
  配对差 −0.0001 ± 0.0014，**t=−0.07, p=0.942**，闭环更优占 53%；
- N=200 迁移: 16 实例 x 8 种子 = 128 配对：差 −0.0010 ± 0.0012，
  **t=−0.89, p=0.375**，更优占 50%；
- 早前 8 实例审计的 −0.0046/−0.0012 为抽样噪声；原报告 Δ=−0.0037 及"方差最小"
  在干净评估下不成立（std 差 0.0001-0.0003，best 打平或略差）；
- **结论：闭环策略相对温度退火的均值改善在足够样本下为零。原声称撤除。**

## 终审后全部声称的状态（2026-09-02）

**已撤（审计至无效或伪差）**：动量均值/TTS 支配（dt 伪差）；动量 best≈Parisi
（等时间未复现）；闭环 μ-策略增益（p=0.94/0.38）；两阶段沉降（修的是无价值
动力学的问题）；全部草案/初始/重启类（击杀测试）；GRPO-v2"收敛更优"（归因混杂，
仅 3× 提速本身有效）。

**存活**：① 复现论文核心（温度退火>增益退火，同协议干净）；② 开环学习饱和于
解析调度（背书方向对泄漏鲁棒）；③ 动量=速度-可靠性权衡的量化（1.3-2.9× /
−20-40pp，honest_rerun 数据）；④ 工程件（smomentum_fast 2-13× 逐位一致、
GRPO-v2 3×、QUBO-QLIB 口径 6/6）；⑤ 负结果图谱（6 实验一致：外部提议无杠杆）；
⑥ QUBO-QLIB 服务器跑分 = 唯一存活的潜在正结果（digCIM 自败 6 题，闭 1 即超）。
