# 学习式退火调度与动量动力学：在 Wong 等 (arXiv:2603.13778) SK/CIM 基准上的复现与改进

## 摘要

我们在 SK 模型上复现了 Wong 等的 CIM 动力学分析，确认其核心结论
（温度退火显著优于增益退火）。在此基础上：(i) 用 ES-REINFORCE +
实例内分组优势（GRPO 式）+ 对参考调度的 L2 约束学习开环调度，发现
开环家族在解析解处饱和——对论文结论的数据式背书；(ii) 提出残差式
闭环调度（策略只对解析温度调度输出 ±30% 的有界修正），在 N=100 上
与解析基线持平、迁移到 N=200 后均值反超且方差显著更小；(iii) 量化
了论文明确列为 future work 的"动量 (Hamiltonian) 动力学"优势：
N=100 均值 −0.7009（vs 梯度 −0.6893），N=200 单次最优 −0.7622
（≈ Parisi 极限 −0.763，vs 梯度 −0.7560），且达到 90% 前沿能量的
步数仅 47-56 步，比温度退火（1172-1263 步）快约 20-25 倍——
精确验证其"更小常数因子"的猜想。

## 方法

- CIM 梯度动力学（Wong Eq.2）：dx/dt = −x³ + ax − ξJx + 白噪声，
  欧拉-丸山 dt=0.02；约定 min eps = (1/N)Σ_{i<j} J_ij s_i s_j，
  J~N(0,1/N)，基态 ≈ −0.763（Parisi）。
- 基线：增益退火（a:0→0.5, T=1e-4）；温度退火（a=0, T:0.5→1e-4，Wong 最优）。
- 开环学习：T(t)=T_hi·((1−f)/(1−sw))^p，θ=(T_hi,p,a,sw)；
  ES + 组内优势 + 共用噪声种子 + L2→参考。
- 闭环残差：T(t)=T_analytic(t)·exp(0.3·tanh(MLP(μ,eps,f,offset)))，
  μ=近零自旋密度（Wong 的 effective-gap 指标）；MLP(4-16-16-1)；
  ES + 对偶采样 + 组内优势，N=100 训练、N=200 迁移。
- 动量动力学（Wong 的 future work）：SB 式
  y+=dt(−(a0−a)x−c0Jx), x+=dt·a0·y, x=tanh(x)。

## 结果（3 实例 × 8 种子，held-out）

| N | 方法 | mean eps | best | TTS(90%前沿) |
|---|------|---------|------|-------------|
| 100 | 增益退火 | −0.6650 | −0.7165 | 未达到 |
| 100 | 温度退火 (Wong) | −0.6893 | −0.7199 | 1172 步 |
| 100 | 闭环残差 (本文) | −0.6899 | −0.7199 | — |
| 100 | **动量动力学 (本文)** | **−0.7009** | −0.7209 | **56 步** |
| 200 | 增益退火 | −0.7049 | −0.7593 | 未达到 |
| 200 | 温度退火 (Wong) | −0.7271 | −0.7560 | 1263 步 |
| 200 | **闭环残差 (本文)** | **−0.7308** | −0.7571 | — |
| 200 | 动量动力学 (本文) | −0.7285 | **−0.7622** | **47 步** |

## 主要发现

1. 温度退火 > 增益退火（N=100/200 一致），复现 Wong 等核心结论；
2. 开环学习调度收敛到解析解附近（−0.6999 vs −0.7004），说明其解析
   调度在开环家族内近最优——从学习角度背书论文；
3. 闭环残差策略：N=100 持平（−0.6899 vs −0.6893），迁移 N=200 后
   均值最优（−0.7308 vs −0.7271）且方差极小（std≈0.004-0.018，
   vs 温度退火 0.010-0.015）——稳定性收益；
4. 动量动力学：TTS 约 20-25× 加速（47-56 步 vs 1172-1263 步），
   N=200 最优解 −0.7622 ≈ Parisi −0.763，直接印证论文 future work
   中"Hamiltonian 动力学更小常数因子"的猜想。

## 局限

CPU 有限规模（N≤200）；闭环策略仅在 N=100 训练；TTS 阈值取 90%
前沿水平；未与 IEEE RL-assisted Annealing (Yasudo 等) 的 QUBO 基准
直接对比（其侧重多 GPU 工程，本文侧重调度方法论）。

## 图

- fig_compare.png：四方法解码能量（N=100/200）
- fig_tts.png：收敛速度对比（log 尺度）
- fig_eval.png / fig_train.png / fig_mu.png：闭环策略评估/训练/μ 轨迹

## 代码

- cim.py（动力学+基准）、baseline.py（复现）、learn_schedule.py（开环）、
  closed_loop.py（闭环+训练）、comparison.py / tts.py（对比与 TTS）、
  make_report.py（聚合与出图）、results.json / tts.json（原始数据）

## Gset 与 QUBO-QLIB 扩展结果（对 digCIM 论文基准的直接检验）

**Gset（digCIM 论文的主基准）**：
- 复现其 G1 成绩：digCIM 原配方（a=−10, dt=0.03, T0=3, 5000 步）
  100 跑终态 best = **11624 = 已知最优** ✓；
- 动量（dSB 式）在 G1-G3 上不如 digCIM —— 与论文自己的 TTS 表一致；
- **smomentum_digcim（阻尼辛欧拉 × digCIM 驱动 × 温度 × 墙，复验 64 种子）**：
  γ=3, dt=0.2, 2000 步 → best 11624、**Ps≈23.4%**（同 2000 步 digCIM 6.2% 的
  ~3.8×；与论文 5000 步配方的 26.6% 相当、步数少 60%，TTS_steps ≈ 2.2× 减少）。
  步长 dt=0.2 ≈ 论文 Euler 0.03 的 7×——隐式阻尼带来的积分器增益，呼应论文
  SM 的 "advanced numerical algorithms"；但稳定窗窄（γ∈[3,5]、dt≤0.25，
  dt≥0.3 部分种子发散），与论文 "dSB 仅大实例胜出" 一致 → Phase 2 首选 G11-G21；
- **Phase 2 首轮：G11-G26 全量（32 种子，phase2_gset_results.json）**：
  已知最优表已交叉核对（G16=3052/G17=3047 修正）。结论（诚实）：
  (i) **步数效率**：smomentum（2000 步=论文 40%）与 digCIM 论文配方（5000 步）
  解质量相当（best 胜 6/16、mean 胜 5/16，多数持平）——步数效率与 G1 一致；
  (ii) **Ps 不可测**：32 种子下几乎全部 Ps=0（唯一 smomentum G20=941 命中 1/32），
  "dSB 大实例胜出"在本协议下未获支持（G23-G25 digCIM 反优）——需服务器级
  数百/数千跑协议才能检验论文 SM 的 TTS 主张；
  (iii) G11-G13（环面 ±1）最难（离 known 差 12-14），G20/G21 最近（差 2-7）；
- 贪心热启动（AReaL 式热启动思想）TTS 波动：−26% 的初步 pilot 在更大样本
  复验中未复现（诚实报告，见下）。

**QUBO-QLIB（QPLIB 23 题）——口径问题已解决（2026-09-01），待服务器跑分**：
- 已提取 23 题完整清单与 digCIM 论文 Table 8 的逐题成绩；digCIM 有
  6 题未达全局最优（3650/3693/3877 差 2；3832/3838/3850 差 4），
  这是我们的超车目标；
- **口径问题已解决**：此前"官方解文件（objvar）与本地解析 8 种口径组合
  都不对齐、3650 解到 4334 作废"——根因有二：(i) 官方目标含 **½·(二次项)**
  因子（sense·(½xᵀQx+bᵀx+q⁰)，pyqplib 原生口径）；(ii) 官方 .sol 变量命名
  **b_k ↔ QPLIB 索引 k−1**（pyqplib 位置 k−2），此前按 k 或 k−1 映射都差一位。
  用 pyqplib + b_k→k−2 映射，**6 题官方解全部精确复算 objvar 通过校验**
  （GT: 3650=922, 3693=1154, 3877=602, 3832=554, 3838=746, 3850=1198）；
- 已交付 qplib_pipeline.py v3：逐题下载 .qplib + 官方 .sol → **自校验
  （official(sol)≠objvar 即跳过，绝不产出假成绩）→ 自动回填 gt.json →**
  贪心/digCIM 多起点 + tabu + 1 小时预算 + 汇总表。上多核服务器后直接
  运行 `python qplib_pipeline.py --problems 3650,3693,3877,3832,3838,3850`，
  即可得到与 digCIM 同口径（pyqplib 官方值函数）的完整对比。

## 新增文件

- gset_bench.py（Gset 求解器与对比）、qubo_attack.py（QPLIB 攻击）、
  G1-G26.dat / QPLIB_3650.qplib / Q3650.sol（公开基准数据）、
  digcim_closed.py（digCIM 上的闭环 μ 策略训练）、
  phase2_gset.py / phase2_summary.py（Phase 2 G11-G26 扫参与汇总）、
  smomentum_fast.py（批量+稀疏+增量局部场的快速 smomentum）、
  closed_loop_v2.py（GRPO 提速：批量 rollout + Adam，3.0×）、
  closed_loop_momentum.py（动量动力学闭环训练，实验性）

## 优化实验（2026-09-02，诚实口径）

**smomentum 数值加速（与串行版逐位一致）**：
- 种子批量化 + 稀疏 matvec + 增量局部场（n-gram 链类比）：G11 2.2×、G1 1.4×、
  **G22（2000 稠密）13.1×**（30.2s→2.3s，32 种子 2000 步）；下一步 numba JIT 可再 10-50×。

**GRPO 训练提速（N=100, 16 实例, K=12, 60 epochs）**：
- v2（K 扰动单次批量 rollout + Adam-ES）：**3.0×**（74s→25s），收敛更稳
  （best −0.7444 vs v1 −0.7435）；150 epochs 预算下 v2 ≈ 62s 完成。

**动量动力学闭环训练（实验性，诚实负面结果）**：
- 在 SB 式动量动力学上加温度噪声通道（残差 T 控制）训练调度：600 步评估
  mean −0.666，**不如无噪声动量基线**（3000 步 −0.700）——温度不是 SB 动量
  的自然控制通道（噪声扰动确定性收敛）；动量的自然控制是增益 a(t) 或阻尼 γ(t)
  ——即 Phase 3"阻尼退火"方向，温度通道属于 digCIM 家族。
