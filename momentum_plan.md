
## Phase 0-1 进度（2026-08，已完成）

**M0 正确性锚点 ✓**
- γ=0 精确复现弹道 SB（SK N=100: mean −0.7104/best −0.7209，与历史数据一致）
- γ→∞ 平滑减速（翻转率 0.0023→0.0015 单调降），时间重标度后等价梯度动力学
- 首个甜点信号：γ=0.3 轻阻尼 mean −0.7134 > 弹道 −0.7104

**Phase 1 G1 诊断 ✓（关键发现）**
- 弹道动量在 G1 失败机理：末段动能 2909（剧烈震荡）+ μ=0（无法锁定）；
  且对比不公平——缺 digCIM 的数字化驱动项与温度噪声
- **smomentum_digcim（阻尼动量 × digCIM 驱动项 × 温度退火 × 墙）G1 复验（64 种子）**：
  best = **11624 = 已知最优可达**；最优设置 γ=3, dt=0.2, 2000 步 → **Ps≈23%**
  （同 2000 步 digCIM 的 6% 高 ~3.8×；与论文 5000 步配方的 26.6% 相当、步数少 60%）
  → 步长 dt=0.2（论文 Euler 0.03 的 ~7 倍）、2000 步（论文 5000 步的 40%）
  → **TTS_steps 约 2.2× 减少** = 论文 SM "advanced numerical algorithms" 的适度定量兑现
  → **诚实更正（2026-09-01 复验）**：此前"16 种子 0% 失败率、γ∈[3,8]×dt∈[0.2,0.4]
    宽稳定窗"不成立——实际稳定窗窄（γ∈[3,5]、dt≤0.25，dt≥0.3 部分种子发散、
    γ≥8 成功率降到 <8%），且"~20 倍"是 SK 动量 TTS 的数字，不能外推到 G1；
    这反而与论文"dSB 仅在大实例胜出"一致 → Phase 2 首选扩 G11-G21

## Phase 2 进度（2026-09-01 晚，进行中）

**Gset 扩实例（进行中，脚本已跑）**
- 已下载 G11-G26（Stanford 源，无扩展名 G11..G26 → 存为 G*.dat）；
  注意 **G11-G21 是带符号权重（±1）**（G1-G3 全 +1）——cut() 公式对带符号图
  仍正确（m=A.sum()/2 − sᵀAs/4），文献已知最优（G11=564 … G21=931）即此口径；
- `phase2_gset.py`：digCIM 论文配方（5000 步基线）× smomentum 扫参
  （γ∈{3,5}×dt∈{0.2,0.25,0.3}，2000 步），16/32 种子同种子、终态口径、
  双口径（终态/running-best）+ 劣解率 + TTS_steps，同 G1 协议可对比；
- 覆盖 G11-G21（800 节点稀疏族）+ G22-G26（2000 节点稠密大实例，
  dSB 理论赢区）；`phase2_summary.py` 出对比表；
- **已知最优表已交叉核对**（p-bit SA 2601.15561 Tab.1 + Adam-for-AIM 2606.03917
  标准表）：G11=564 G12=556 G13=582 G14=3064 G15=3050 **G16=3052 G17=3047**
  G18=992 G19=906 G20=941 G21=931 G22=13359 G23=13344 G24=13337 G25=13340
  G26=13328（初版 552/550 为记忆错误，已修正）；
- ✅ **首轮结果（32 种子，G11-G26 全量，phase2_gset_results.json）**：
  - **步数效率成立**：smomentum（2000 步）以 60% 步数达到与 digCIM 论文配方
    （5000 步）相当的解质量——best 胜出 6/16、mean 胜出 5/16（其余持平/微负），
    与 G1 发现一致；
  - **Ps 口径不可测**：32 种子下两种方法在 G11-G26 几乎全部 Ps=0（唯一例外
    smomentum G20 γ=3,dt=0.3 命中 941=known，1/32）；"dSB 大实例胜出"假设
    **未获支持**（G23-G25 digCIM 反优）——需论文级数百/数千跑协议（服务器）才能检验；
  - **注意**：G11-G21 为带符号权重（±1），G14-G17 全 +1；digCIM 在 G11-G13
    （环面 ±1）离 known 最远（差 12-14），G20/G21 最近（差 2-7）；
